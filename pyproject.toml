[project]
name = "urlshortener"
version = "0.1.0"
description = "Useful service for creating short urls"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.129.0",
    "sqlalchemy>=2.0.46",
]
